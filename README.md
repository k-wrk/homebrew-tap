# homebrew-tap

Personal Homebrew Tap for **k-wrk** projects.

## How to Install My Formulas

To install any tool from this tap, run:

```bash
brew tap k-wrk/tap
brew install <formula>
```

For example, to install **Cinder**:

```bash
brew install cinder
```

